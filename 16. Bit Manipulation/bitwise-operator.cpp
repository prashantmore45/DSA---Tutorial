#include<iostream>
using namespace std;

int main() {
    cout << (3 & 5) << endl;  // Bitwise AND
    cout << (3 | 5) << endl;  // Bitwise OR
    cout << (3 ^ 5) << endl;  // Bitwise XOR
    cout << (~3) << endl;     // Bitwise NOT
    return 0;
}